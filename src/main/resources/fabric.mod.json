{
  "schemaVersion": 1,
  "id": "chainmail_bucket",
  "version": "${version}",
  "name": "Chainmail Bucket",
  "description": "Adds a bucket that is only able to remove water source blocks, and not store it",
  "authors": [
    "Andrew6rant (Andrew Grant)"
  ],
  "contact": {},
  "license": "MIT",
  "icon": "assets/chainmail_bucket/icon.png",
  "environment": "*",
  "entrypoints": {
    "client": [
      "io.github.andrew6rant.chainmail_bucket.client.ChainmailBucketClient"
    ],
    "main": [
      "io.github.andrew6rant.chainmail_bucket.ChainmailBucket"
    ],
    "fabric-datagen": [
      "io.github.andrew6rant.chainmail_bucket.ChainmailBucketDataGenerator"
    ]
  },
  "mixins": [
    "chainmail_bucket.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.15",
    "fabric": "*",
    "minecraft": ">=1.21.5"
  }
}
